mutation($idRoom : ID! , $idDoctor : ID!){
      AddRoomDoctor(
        from:{
            id:$idRoom
        },
        to:{
            id:$idDoctor
        }
      ){
      from{id},
      to{id}
    },
}