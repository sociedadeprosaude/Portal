query($email:String!){
  Colaborator(filter:{email:$email}){
    id,
    name,
    cpf,
    email,
    group,
    salary,
    permissions,
    birth_date{formatted},
    clinic:work_in{
      id,
      name,
      logo
    }
  }
}