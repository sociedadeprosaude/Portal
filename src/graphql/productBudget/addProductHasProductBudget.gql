mutation($idBudgetProduct:ID!, $idProduct:ID!){
    AddProductBudgetWith_product(
        from:{
            id:$idBudgetProduct
        },
        to:{
            id:$idProduct
        }
    ){
        from{id},
        to{id}
    },
}