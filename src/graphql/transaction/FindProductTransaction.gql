query($idPatient:ID!,$idProduct:ID!){
  ProductTransaction(filter:{with_transaction:{with_user:{id:$idPatient}},with_product:{id:$idProduct}, with_consultation:null}){
    id,
    with_product{
      name
    }
  }
}