mutation($idTicket : ID! , $idColaborator : ID!){
       AddTicketUser(
        from:{
          id: $idTicket
        },
        to:{
          id: $idColaborator
        }
      ){
      from{id},
      to{id}
    },
}